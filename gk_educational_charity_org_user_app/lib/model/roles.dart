class Roles {
  String id;
  bool isActive;
  bool isAdmin;
  String name;

  // Constructor
  Roles({
    required this.id,
    required this.isActive,
    required this.isAdmin,
    required this.name,
  });
}
